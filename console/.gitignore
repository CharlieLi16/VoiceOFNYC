.venv/
backend/.env
backend/data/google_oauth_token.json
data/*.db
__pycache__/
*.pyc
.venv/
venv/
frontend/node_modules/
frontend/dist/
frontend/.env.local

# Firebase CLI 调试日志
firebase-vote/firebase-debug.log

# Firebase Functions 本地参数（含 VOTE_INGEST_SECRET，勿提交）
firebase-vote/functions/.env
firebase-vote/functions/.env.*
!firebase-vote/functions/.env.example
firebase-vote/scripts/node_modules/
# 投票码导出（勿提交仓库）；vote-codes.csv 例外——签到池 / Docker COPY 依赖，须进 Git 才能在 Railway 构建
firebase-vote/data/*.csv
!firebase-vote/data/vote-codes.csv

# Local service account / keys (never commit)
key/

# Windows alternate data stream artifacts under WSL
*Zone.Identifier

# 本机汇总的敏感环境变量文档（勿提交）
docs/ENVIRONMENT-VALUES.local.md
