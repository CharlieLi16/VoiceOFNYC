# 复制为 .env 供本机 deploy / emulator（勿提交 .env）
# 与 Secret Manager 中其它变量一并配置到 Firebase 亦可。

# 可选。不写或留空 = 发布不校验。若环境里仍有旧密钥，可显式关闭：
# STAFF_PUBLISH_SECRET=DISABLED
# 若设其它非空字符串，调度页须填同一串。
# STAFF_PUBLISH_SECRET=

# 测试投票码：非空时 Callable 接受该码（不分大小写），且不扣 Firestore tickets。
# 与前端 vote-config.js 的 testVoteCode 保持一致。正式现场务必留空或删除。
# VOTE_TEST_CODE=CSSA-VOTE-TEST
