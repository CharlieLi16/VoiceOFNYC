# 复制为 .env.local 后填写（勿提交密钥）
# 用 python http.server 等静态托管 dist 时，指到 FastAPI 根地址（无尾斜杠）
# VITE_API_BASE=http://127.0.0.1:8765

# 非空则启用控制台门户：未登录只能打开 /login；观众投票仍为 /vote/*.html（静态）
# VITE_STAFF_PORTAL_PASSWORD=

VITE_GOOGLE_SHEET_ID=
VITE_GOOGLE_SHEETS_API_KEY=
