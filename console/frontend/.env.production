# 本机 `npm run build` 会读取本文件（已在 console/.gitignore，勿提交仓库）。
#
# Vercel：Project → Settings → Environment Variables，添加同名变量：
#   VITE_API_BASE = 公网可访问的 FastAPI 根地址（https://... 无尾斜杠，勿用 localhost）
#   VITE_GOOGLE_SHEET_ID / VITE_GOOGLE_SHEETS_API_KEY = 与 .env.local 一致（大屏读表用）
#
# 本地预览 dist：保持下面为 127.0.0.1；连远程后端时改成你的公网 API 再 build。

VITE_API_BASE=http://127.0.0.1:8765
